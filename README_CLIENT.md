1) Install Docker Desktop.
2) Double-click `Start.bat`.
3) The app opens at: http://localhost:8080 (Therapy Portal + SMS Calendar).
4) To stop, double-click `Stop.bat`.
5) Optional advanced: `docker compose up -d --build` / `docker compose down`.
